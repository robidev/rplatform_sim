using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IMUController : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		//3axis accel to know heading/speed/accel
		//3axis gyro to know orientation
		//3axis magneto for orientation


		//send rotation/orientation in angles
		//send rotation speed
		//send compass-reading(world north)
		//send acceleration vector(gravity+movement)
		//send lineair acceleration vector(minus gracity)
		//send gravity vector(minus movement)
		//
	}
}
